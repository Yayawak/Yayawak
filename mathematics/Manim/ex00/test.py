from manimlib import *
import cv2
import numpy as np

# class CreateCircle(Scene):
class CircleToSquare(Scene):
    def construct(self):
        circle = Circle()
        # circle.set_fill(PINK, opacity=.5)
        # # self.play(Create(circle))
        # self.add(circle)
        # circle.
        # self.play(Write(Tex(u"f(x) = a + bx + cx^2")), run_time=3)
        self.play(Write(Text("f(x) = a + bx + cx^2")), run_time=3)

